#include<cstdio>
#include<cstring>
#define maxn 10000
using namespace std;
int main()
{
    char str[maxn];
    int n;
    scanf("%d",&n);
    if(n==0)
        return 0;
    while(n--)
    {
        scanf("%s",str);

    }

}
